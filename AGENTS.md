# Repo Dependencies
- **Codex CLI**: This project uses a locally built `codex` binary from `/Users/mrs/Desktop/typescript/multi-model-codex/multi-model-codex/`. Treat that repo as the source of truth for Codex behavior and fixes; investigate Codex issues there.
- **NanoClaw**: `/Users/mrs/Desktop/typescript/nanoclaw` is used to let Dawn communicate with users via WhatsApp. It is otherwise a separate assistant codebase.
